<%= form_with(model: meteo, local: true) do |form| %>
  <% if meteo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meteo.errors.count, "error") %> prohibited this meteo from being saved:</h2>

      <ul>
      <% meteo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
